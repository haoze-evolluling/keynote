<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <queries>
        <intent>
            <action android:name="android.intent.action.SET_ALARM" />
        </intent>
    </queries>

    <application
        android:name=".KeyNoteApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KeyNote">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.KeyNote.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NotesActivity" />
        <activity android:name=".EditNoteActivity" />
        <activity android:name=".BillActivity" />
        <activity android:name=".BillStatsActivity" />
        <activity android:name=".AaSplitActivity" />
        <activity android:name=".HabitActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".AiProviderManageActivity" />
        <activity android:name=".TodoActivity" />
        <activity android:name=".TagNotesActivity" />
        <activity android:name=".KnowledgeVaultActivity" />
        <activity android:name=".DateGroupNotesActivity" />
        <activity android:name=".TrashActivity" />
        <activity android:name=".DataExportActivity" />
        <activity android:name=".ScheduleActivity" />

        <receiver
            android:name=".receiver.TrashCleanupReceiver"
            android:exported="false" />
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:exported="false" />
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>

